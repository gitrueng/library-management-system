spring.application.name=library-backend
spring.datasource.url=jdbc:postgresql://localhost:1234/librarydb
spring.datasource.username=postgres
spring.datasource.password=sophia
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

## Change the server port form default (8080) to custom number (e.g. 8081)
# server.port=8081

## This file is written in "property format" not "Java"
## "property format" is a configuration file format
## "Java" is a programming language

